package version

var (
  // BuildTime: Moment when binary was built.
  BuildTime = "unset"
  // Commit: Last commit hash.
  Commit = "unset"
  // Release: Semantic version of current build.
  Release = "unset"
)
